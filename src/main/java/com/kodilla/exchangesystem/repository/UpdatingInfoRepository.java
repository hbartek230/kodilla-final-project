package com.kodilla.exchangesystem.repository;

import com.kodilla.exchangesystem.domain.UpdatingInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UpdatingInfoRepository extends JpaRepository<UpdatingInfo, Long> {
}
